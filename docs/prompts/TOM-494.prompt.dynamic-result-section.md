# ⚡ AI 可执行 Prompt (Executable Prompt)

**任务ID:** `TOM-494: Web: 重构ResultSection以支持"动态结构化"渲染`  
**创建时间:** 2025-11-11  
**规范版本:** v2.2 (Agentic & Backend-First)

---

## 🎯 目标 (Objective)

**Linear Issue**: TOM-494 - Web: 重构ResultSection以支持"动态结构化"渲染

**核心指令**: 重构 Web App (Next.js) 的 `ResultSection` 组件，实现一个"动态渲染器"，使其能够根据后端 `TOM-492` 返回的 `schema_type` 字段，渲染不同结构的 UI（V2.0 通用叙事 vs V3.0 科技评测）。**必须遵循 TDD 原则，测试先行。**

---

## 📋 上下文参考 (Context References)

### 主要操作文件

#### 待修改文件 (已存在)
- `@/src/components/sections/ResultSection.tsx` - 主要重构文件
- `@/src/components/sections/ResultSection.test.tsx` - 测试文件（需更新）
- `@/src/types/script-parser.types.ts` - 类型定义文件
- `@/src/stores/app-store.ts` - Zustand 状态管理

#### 待创建文件 (新增)
- `@/src/components/cards/ProductParametersCard.tsx` - V3.0 产品参数卡片
- `@/src/components/cards/SellingPointsCard.tsx` - V3.0 卖点卡片  
- `@/src/components/cards/PricingCard.tsx` - V3.0 价格信息卡片
- `@/src/components/cards/ProsConsCard.tsx` - V3.0 优缺点评价卡片

### 需要遵循的内部规则

- `@.cursor/rules/001-gen-constitution-principles.mdc` (核心铁律)
- `@.cursor/rules/005-spec-definition-of-done.mdc` (完成标准)
- `@.cursor/rules/010-gen-coding-standards.mdc` (编码标准)
- `@.cursor/rules/011-gen-naming-conventions.mdc` (命名规范)
- `@.cursor/rules/020-gen-typescript-best-practices.mdc` (TS 最佳实践)
- `@.cursor/rules/040-spec-visual-design-principles.mdc` (视觉规范)
- `@.cursor/rules/121-spec-frontend-error-handling.mdc` (错误处理)
- `@.cursor/rules/130-spec-testing-guide.mdc` (TDD/Mocking 规范)
- `@.cursor/rules/140-spec-frontend-state-management.mdc` (状态管理规范)

### 参考文档

- `@/docs/development/技术方案 - 脚本快拆-V2.0 (V3.0 MVP版).md` (V2.1 融合架构蓝图)

---

## 🔧 具体要求与约束 (Requirements & Constraints)

### 技术栈约束

- **语言**: TypeScript (strict mode)
- **框架**: Next.js 14 (App Router)
- **状态管理**: Zustand
- **UI 组件库**: shadcn/ui
- **样式**: Tailwind CSS
- **测试框架**: Jest + React Testing Library

### 功能要求

#### 1. 类型定义扩展 (Spec 1)

**当前类型** (`script-parser.types.ts`):
```typescript
export type AnalysisResult = {
  readonly raw_transcript: string
  readonly cleaned_transcript: string
  readonly analysis: {
    readonly hook: string
    readonly core: string
    readonly cta: string
    readonly key_quotes?: readonly string[]
  }
}
```

**新增类型** (V3.0 Tech Spec):
```typescript
// V3.0 科技评测输出结构
export type V3TechSpecOutput = {
  readonly schema_type: 'v3_tech_spec'
  readonly product_parameters: ReadonlyArray<{
    readonly parameter: string
    readonly value: string
  }>
  readonly selling_points: ReadonlyArray<{
    readonly point: string
    readonly context_snippet: string
  }>
  readonly pricing_info: ReadonlyArray<{
    readonly product: string
    readonly price: string
    readonly context_snippet: string
  }>
  readonly subjective_evaluation: {
    readonly pros: readonly string[]
    readonly cons: readonly string[]
  }
}

// V2.0 通用叙事输出结构（重命名以明确区分）
export type V2NarrativeOutput = AnalysisResult & {
  readonly schema_type?: 'v2_narrative' // 可选，用于明确标识
}

// 联合类型：后端可能返回的两种结构
export type DynamicAnalysisResult = V2NarrativeOutput | V3TechSpecOutput
```

**要求**:
- 严格遵循 TypeScript 最佳实践（readonly、联合类型）
- 不使用 `any` 类型
- 所有字段必须明确类型

#### 2. 状态管理更新 (Spec 2)

**修改 `app-store.ts`**:
```typescript
export interface AppStore {
  // State
  resultData: DynamicAnalysisResult | null; // 从 AnalysisResult 改为 DynamicAnalysisResult
  
  // 其他字段保持不变
  // ...
}
```

**要求**:
- 保持向后兼容（V2.0 用户不受影响）
- 不破坏现有测试

#### 3. 动态渲染逻辑 (Spec 3)

**`ResultSection.tsx` 重构要求**:

```typescript
export function ResultSection({ result, onReset }: ResultSectionProps) {
  // 类型守卫 (Type Guard)
  const isV3TechSpec = (data: DynamicAnalysisResult): data is V3TechSpecOutput => {
    return 'schema_type' in data && data.schema_type === 'v3_tech_spec'
  }

  const isV2Narrative = (data: DynamicAnalysisResult): data is V2NarrativeOutput => {
    return 'analysis' in data && 'hook' in data.analysis
  }

  // 动态渲染逻辑
  if (isV3TechSpec(result)) {
    return <V3TechSpecLayout result={result} onReset={onReset} />
  }

  if (isV2Narrative(result)) {
    return <V2NarrativeLayout result={result} onReset={onReset} />
  }

  // 错误状态：未知的 schema_type
  return <ErrorState message="未知的分析结果格式" onReset={onReset} />
}
```

**要求**:
- 使用 TypeScript 类型守卫（Type Guards）
- 拆分为两个子组件：`V2NarrativeLayout` 和 `V3TechSpecLayout`
- 保持现有 V2.0 布局不变
- 新增 V3.0 布局（参考规格 4）

#### 4. V3.0 卡片组件 (Spec 4)

**必须创建的 4 个新组件**:

##### a. `ProductParametersCard.tsx`
```typescript
type ProductParametersCardProps = {
  readonly parameters: ReadonlyArray<{
    readonly parameter: string
    readonly value: string
  }>
  readonly onCopy: (text: string, type: string) => void
}

export function ProductParametersCard({ parameters, onCopy }: ProductParametersCardProps) {
  // 渲染产品参数表格
  // 使用 shadcn/ui Table 组件
  // 每行参数可复制
}
```

**UI 要求**:
- Icon: `<Cpu className="h-4 w-4 mr-2 text-blue-500" />` (from lucide-react)
- 布局: 表格形式（参数名 | 参数值）
- 交互: 每行有复制按钮

##### b. `SellingPointsCard.tsx`
```typescript
type SellingPointsCardProps = {
  readonly sellingPoints: ReadonlyArray<{
    readonly point: string
    readonly context_snippet: string
  }>
  readonly onCopy: (text: string, type: string) => void
}

export function SellingPointsCard({ sellingPoints, onCopy }: SellingPointsCardProps) {
  // 渲染卖点列表
  // 每个卖点显示: 卖点 + 原文引用（折叠/展开）
}
```

**UI 要求**:
- Icon: `<Sparkles className="h-4 w-4 mr-2 text-yellow-500" />` (from lucide-react)
- 布局: 列表形式，每个卖点独立卡片
- 交互: 卖点可复制，引用文本可展开/折叠

##### c. `PricingCard.tsx`
```typescript
type PricingCardProps = {
  readonly pricingInfo: ReadonlyArray<{
    readonly product: string
    readonly price: string
    readonly context_snippet: string
  }>
  readonly onCopy: (text: string, type: string) => void
}

export function PricingCard({ pricingInfo, onCopy }: PricingCardProps) {
  // 渲染价格信息列表
}
```

**UI 要求**:
- Icon: `<DollarSign className="h-4 w-4 mr-2 text-green-500" />` (from lucide-react)
- 布局: 列表形式，产品名 + 价格突出显示
- 交互: 价格信息可复制

##### d. `ProsConsCard.tsx`
```typescript
type ProsConsCardProps = {
  readonly evaluation: {
    readonly pros: readonly string[]
    readonly cons: readonly string[]
  }
  readonly onCopy: (text: string, type: string) => void
}

export function ProsConsCard({ evaluation, onCopy }: ProsConsCardProps) {
  // 渲染优缺点评价
  // 分为两列：优点（绿色）| 缺点（红色）
}
```

**UI 要求**:
- Icon: `<ThumbsUp className="h-4 w-4 mr-2 text-green-500" />` (优点) / `<ThumbsDown className="h-4 w-4 mr-2 text-red-500" />` (缺点)
- 布局: 两列布局（优点左 | 缺点右）
- 交互: 每个优缺点可独立复制

**所有卡片的通用要求**:
- 严格遵循 `@rules/040-spec-visual-design-principles.mdc`
- 使用 `Card`, `CardHeader`, `CardTitle`, `CardContent` from shadcn/ui
- 间距: 所有 margin/padding 必须是 4px 的倍数
- 响应式: Mobile-first 设计
- 可访问性: 正确使用 ARIA 属性

#### 5. V3.0 布局设计 (Spec 5)

**`V3TechSpecLayout` 组件结构**:

```
┌─────────────────────────────────────────────────┐
│             Header (分析完成)                    │
└─────────────────────────────────────────────────┘

┌──────────────────┬──────────────────────────────┐
│  Left Column     │  Right Column                │
│  (2/4 width)     │  (2/4 width)                 │
│                  │                              │
│  ┌────────────┐  │  ┌────────────────────────┐ │
│  │ Action     │  │  │ ProductParametersCard  │ │
│  │ Panel      │  │  └────────────────────────┘ │
│  └────────────┘  │                              │
│                  │  ┌────────────────────────┐ │
│  ┌────────────┐  │  │ SellingPointsCard      │ │
│  │ Pricing    │  │  └────────────────────────┘ │
│  │ Card       │  │                              │
│  └────────────┘  │  ┌────────────────────────┐ │
│                  │  │ ProsConsCard           │ │
│  ┌────────────┐  │  └────────────────────────┘ │
│  │ 全部复制    │  │                              │
│  │ (所有数据)  │  │                              │
│  └────────────┘  │                              │
└──────────────────┴──────────────────────────────┘
```

**要求**:
- 不显示 transcript（科技模式关注数据，不关注逐字稿）
- 左列: 操作按钮 + 快速预览（价格、关键参数）
- 右列: 详细数据卡片（参数、卖点、评价）
- "全部复制"按钮: 将所有 V3.0 数据格式化为 Markdown 并复制

### 类型安全要求 (🛡️ Type Safety)

- **禁止使用 `any`**: 所有类型必须明确定义
- **使用 `readonly`**: 所有对象和数组必须使用 `readonly` 修饰符
- **类型守卫**: 使用 TypeScript 类型守卫（Type Guards）进行运行时类型检查
- **联合类型**: 使用联合类型 (`V2NarrativeOutput | V3TechSpecOutput`) 而非条件类型

---

## 💻 示例与输出结构 (Example & Output Structure)

### 用法示例 (将被转化为测试用例)

> [!example] **动态渲染示例**
> *AI, 你将要实现的功能，最终会被这样调用。你的实现必须能让这个示例成功运行。*
> 
> ```typescript
> // 场景 1: V2.0 通用叙事模式
> const v2Result: V2NarrativeOutput = {
>   raw_transcript: "...",
>   cleaned_transcript: "...",
>   analysis: {
>     hook: "今天评测新手机",
>     core: "这是核心内容",
>     cta: "点赞关注",
>     key_quotes: ["这是金句"]
>   }
> }
> 
> <ResultSection result={v2Result} onReset={() => {}} />
> // 预期: 渲染 V2.0 布局（钩子、核心、CTA 卡片）
> 
> // 场景 2: V3.0 科技评测模式
> const v3Result: V3TechSpecOutput = {
>   schema_type: 'v3_tech_spec',
>   product_parameters: [
>     { parameter: "CPU", value: "M4 Max" },
>     { parameter: "RAM", value: "64GB" }
>   ],
>   selling_points: [
>     { 
>       point: "性能提升30%", 
>       context_snippet: "M4 Max 芯片性能提升30%"
>     }
>   ],
>   pricing_info: [
>     { 
>       product: "MacBook Pro", 
>       price: "$3499", 
>       context_snippet: "起售价$3499"
>     }
>   ],
>   subjective_evaluation: {
>     pros: ["性能强劲", "续航优秀"],
>     cons: ["价格昂贵"]
>   }
> }
> 
> <ResultSection result={v3Result} onReset={() => {}} />
> // 预期: 渲染 V3.0 布局（产品参数、卖点、价格、评价卡片）
> ```

---

## 🧪 测试策略 (Testing Strategy) **(第一步执行)**

### AI角色：软件质量保证工程师 (AI Role: Software QA Engineer)

你将扮演一个专业的、遵循"智能体测试"理念的软件质量保证工程师。你的核心任务是为即将开发的功能生成一套全面、严谨的测试用例。

### 核心原则

- **测试优先级**: 本任务为前端 UI 逻辑，属于组件测试。
- **TDD 流程**: 必须先编写失败的测试，然后实现功能，最后确认所有测试通过。

### 本任务主要测试类型: 组件测试 (Component Tests)

---

### 5.1 组件测试 - ResultSection (动态渲染逻辑)

#### 目标: 验证 ResultSection 能正确识别并渲染两种不同的结果结构

#### 测试文件: `src/components/sections/ResultSection.test.tsx`

#### AI执行要求:

**第一步: 更新测试文件，编写失败的测试用例**

- [ ] **打开现有测试文件** `ResultSection.test.tsx`
- [ ] **保留所有现有测试**（确保向后兼容）
- [ ] **新增以下测试用例**:

##### Test Group 1: V2.0 模式渲染（向后兼容测试）

**Test 1.1**: `should render V2.0 narrative layout when schema_type is undefined`

**Mock 数据**:
```typescript
const v2Result: V2NarrativeOutput = {
  raw_transcript: "...",
  cleaned_transcript: "...",
  analysis: {
    hook: "Test Hook",
    core: "Test Core",
    cta: "Test CTA"
  }
}
```

**断言**:
- 验证 "完整逐字稿" 卡片被渲染
- 验证 "钩子" 卡片被渲染
- 验证 "核心" 卡片被渲染
- 验证 "行动号召" 卡片被渲染
- 验证 **不渲染** V3.0 卡片（ProductParametersCard 等）

##### Test Group 2: V3.0 模式渲染

**Test 2.1**: `should render V3.0 tech spec layout when schema_type is v3_tech_spec`

**Mock 数据**:
```typescript
const v3Result: V3TechSpecOutput = {
  schema_type: 'v3_tech_spec',
  product_parameters: [
    { parameter: "CPU", value: "M4 Max" }
  ],
  selling_points: [
    { point: "性能提升", context_snippet: "..." }
  ],
  pricing_info: [
    { product: "MacBook", price: "$3499", context_snippet: "..." }
  ],
  subjective_evaluation: {
    pros: ["优点1"],
    cons: ["缺点1"]
  }
}
```

**断言**:
- 验证 "产品参数" 卡片被渲染
- 验证 "卖点" 卡片被渲染
- 验证 "价格" 卡片被渲染
- 验证 "优缺点评价" 卡片被渲染
- 验证 **不渲染** V2.0 卡片（钩子、核心、CTA）
- 验证 **不渲染** "完整逐字稿" 卡片

**Test 2.2**: `should render product parameters in a table format`

**断言**:
- 验证参数表格被渲染
- 验证表格包含正确的参数名和参数值
- 验证每行参数有复制按钮

**Test 2.3**: `should render selling points with context snippets`

**断言**:
- 验证卖点列表被渲染
- 验证每个卖点显示了 `point` 和 `context_snippet`
- 验证卖点可以被复制

**Test 2.4**: `should render pricing info correctly`

**断言**:
- 验证价格信息列表被渲染
- 验证显示了产品名、价格和原文引用
- 验证价格信息可以被复制

**Test 2.5**: `should render pros and cons in two columns`

**断言**:
- 验证优点列表被渲染（包含 "优点1"）
- 验证缺点列表被渲染（包含 "缺点1"）
- 验证优缺点可以独立复制

##### Test Group 3: 复制功能（V3.0）

**Test 3.1**: `should copy product parameter when copy button clicked (V3.0)`

**Test 3.2**: `should copy selling point when copy button clicked (V3.0)`

**Test 3.3**: `should copy pricing info when copy button clicked (V3.0)`

**Test 3.4**: `should copy all V3.0 data as markdown when "全部复制" clicked`

**要求**:
- Mock `copy-to-clipboard` 库
- 验证正确的文本被传递给 `copy()` 函数
- 验证 Toast 通知被调用

##### Test Group 4: 类型守卫测试

**Test 4.1**: `should correctly identify V2 narrative result`

**Test 4.2**: `should correctly identify V3 tech spec result`

- [ ] **运行测试**: `cd /Users/liumingwei/01-project/12-liumw/14-script-parser/apps/web && pnpm test ResultSection`
- [ ] **确认所有新测试失败** (因为新组件和逻辑尚未实现)

---

### 5.2 组件测试 - V3.0 卡片组件

#### 测试文件: `src/components/cards/__tests__/ProductParametersCard.test.tsx` (新建)

**测试用例**:
- [ ] `should render all product parameters`
- [ ] `should call onCopy when copy button clicked`
- [ ] `should handle empty parameters array`

**同理创建**:
- `SellingPointsCard.test.tsx`
- `PricingCard.test.tsx`
- `ProsConsCard.test.tsx`

---

## 🔄 实现步骤 (Implementation Steps) **(第二步执行)**

### AI执行要求 (强制性)

- [ ] **第1步 (类型定义)**: 打开 `src/types/script-parser.types.ts`
  - 添加 `V3TechSpecOutput` 类型定义
  - 添加 `V2NarrativeOutput` 类型别名
  - 添加 `DynamicAnalysisResult` 联合类型
  - 保持现有 `AnalysisResult` 向后兼容

- [ ] **第2步 (状态管理)**: 打开 `src/stores/app-store.ts`
  - 修改 `resultData` 类型为 `DynamicAnalysisResult | null`
  - 确保所有现有测试仍然通过

- [ ] **第3步 (创建 V3.0 卡片组件)**: 
  - 创建 `src/components/cards/ProductParametersCard.tsx`
  - 创建 `src/components/cards/SellingPointsCard.tsx`
  - 创建 `src/components/cards/PricingCard.tsx`
  - 创建 `src/components/cards/ProsConsCard.tsx`
  - 每个组件必须：
    - 使用 shadcn/ui 组件
    - 遵循 Rondo 设计规范
    - 支持复制功能
    - 包含 TypeScript 类型定义
    - 编写对应的单元测试

- [ ] **第4步 (重构 ResultSection)**: 打开 `src/components/sections/ResultSection.tsx`
  - 实现类型守卫函数 `isV3TechSpec` 和 `isV2Narrative`
  - 创建 `V2NarrativeLayout` 子组件（现有布局）
  - 创建 `V3TechSpecLayout` 子组件（新布局）
  - 实现动态渲染逻辑（主switch/if判断）
  - 处理未知 `schema_type` 的错误状态

- [ ] **第5步 (V3.0 Markdown 导出)**: 创建 `src/lib/utils.ts` 中的新函数
  - `downloadAsMarkdownV3(result: V3TechSpecOutput): void`
  - 格式化 V3.0 数据为 Markdown 格式

- [ ] **第6步 (验证测试)**: 
  - 运行 `pnpm test ResultSection`
  - 确认所有新测试通过（绿灯）
  - 确认所有现有测试仍然通过（向后兼容）

- [ ] **第7步 (Lint 和类型检查)**:
  - 运行 `pnpm lint`
  - 运行 `pnpm type-check` (或 `tsc --noEmit`)
  - 修复所有错误

- [ ] **第8步 (手动测试)**: 
  - 启动开发服务器 `pnpm dev`
  - 测试 V2.0 路径（选择"通用叙事分析"）
  - 测试 V3.0 路径（选择"科技产品评测"）
  - 验证所有交互功能正常

---

## 🎉 完成检查清单 (Completion Checklist)

### Ⅰ. 功能与质量检查 (AI必须执行)

- [ ] 是否严格遵循了"测试先行"的流程？
- [ ] **ResultSection 的组件测试是否全部通过？**
- [ ] **V3.0 卡片组件的单元测试是否全部通过？**
- [ ] **所有现有测试是否仍然通过（向后兼容）？**
- [ ] 是否遵循了 TypeScript 最佳实践（无 `any`，使用 `readonly`）？
- [ ] 是否遵循了 Rondo 视觉设计规范？
- [ ] 是否为所有新增组件编写了 TSDoc 文档？

### Ⅱ. 构建与验证检查 (AI必须执行)

- [ ] **Lint检查是否通过 (`pnpm lint`)？**
- [ ] **TypeScript类型检查是否通过 (`tsc --noEmit`)？**
- [ ] **所有测试是否通过 (`pnpm test`)？**
- [ ] **开发服务器是否正常启动 (`pnpm dev`)？**

### Ⅲ. 流程与交付检查 (AI在最后一步执行)

- [ ] **Git Commit Message是否已按 `@rules/210-wf-git-commit.mdc` 规范生成？**
- [ ] **是否已将本 Prompt 文档链接到 Linear Issue TOM-494 的评论中？**
- [ ] **是否已将实现完成情况添加到 Linear Issue TOM-494 的评论中？**

---

## 📌 重要提醒 (Critical Reminders)

1. **向后兼容优先**: V2.0 用户的体验必须完全不受影响。所有现有测试必须继续通过。
2. **类型安全至关重要**: 使用 TypeScript 的类型守卫，避免运行时类型错误。
3. **渐进式重构**: 先实现类型定义和测试，再逐步实现组件。
4. **视觉一致性**: 所有 V3.0 卡片必须遵循 Rondo 设计规范，保持视觉统一。
5. **可访问性**: 所有交互元素必须有正确的 ARIA 属性和键盘支持。
6. **测试覆盖率**: 新代码的测试覆盖率必须达到 80% 以上。

---

## 🔗 相关资源 (Related Resources)

- **Linear Issue**: [TOM-494](https://linear.app/tomorrow-persistence/issue/TOM-494)
- **依赖任务**:
  - ✅ TOM-489: Web UI 分析模式选择器（已完成）
  - ✅ TOM-492: 后端赛道路由器架构（已完成）
  - ✅ TOM-491: Tech Spec Extraction Prompt（已完成）
- **技术方案**: `@/docs/development/技术方案 - 脚本快拆-V2.0 (V3.0 MVP版).md`
- **设计规范**: `@.cursor/rules/040-spec-visual-design-principles.mdc`
- **测试规范**: `@.cursor/rules/130-spec-testing-guide.mdc`

---

## 📐 V3.0 卡片设计细节 (Design Specifications)

### ProductParametersCard 设计规范

**布局**:
```
┌────────────────────────────────────────┐
│ 💻 产品参数                     [复制全部] │
├────────────────────────────────────────┤
│ ┌──────────────┬─────────────┬───────┐ │
│ │ 参数         │ 值          │ 操作  │ │
│ ├──────────────┼─────────────┼───────┤ │
│ │ CPU          │ M4 Max      │ [📋] │ │
│ │ RAM          │ 64GB        │ [📋] │ │
│ │ 存储         │ 2TB SSD     │ [📋] │ │
│ └──────────────┴─────────────┴───────┘ │
└────────────────────────────────────────┘
```

**样式**:
- 表格: 使用 `shadcn/ui` 的 `Table` 组件
- 间距: 行高 `40px`，内边距 `12px`
- 边框: `border border-border`
- 悬停: 行悬停时背景变为 `bg-muted/50`

### SellingPointsCard 设计规范

**布局**:
```
┌────────────────────────────────────────┐
│ ✨ 核心卖点                             │
├────────────────────────────────────────┤
│ ┌──────────────────────────────────┐   │
│ │ • 性能提升30%              [📋] │   │
│ │   原文: "M4 Max 性能提升30%"     │   │
│ └──────────────────────────────────┘   │
│ ┌──────────────────────────────────┐   │
│ │ • 电池续航优秀            [📋] │   │
│ │   原文: "续航时间可达一整天"     │   │
│ └──────────────────────────────────┘   │
└────────────────────────────────────────┘
```

**样式**:
- 每个卖点: 独立的 mini-card (`bg-muted/50`)
- 原文引用: 可折叠，使用 `Collapsible` 组件
- Icon: `Sparkles` (黄色)

### PricingCard 设计规范

**布局**:
```
┌────────────────────────────────────────┐
│ 💰 价格信息                             │
├────────────────────────────────────────┤
│ MacBook Pro                             │
│ $3,499                          [📋]   │
│ 引用: "起售价$3499"                     │
├────────────────────────────────────────┤
│ MacBook Air                             │
│ $1,299                          [📋]   │
│ 引用: "Air 版本$1299起"                 │
└────────────────────────────────────────┘
```

**样式**:
- 价格: 大字体显示 (`text-2xl font-bold`)
- 颜色: 价格使用 `text-green-600`
- 产品名: `text-base font-medium`

### ProsConsCard 设计规范

**布局**:
```
┌────────────────────────────────────────┐
│ ⚖️ 评测总结                             │
├──────────────────┬─────────────────────┤
│ 👍 优点          │ 👎 缺点             │
├──────────────────┼─────────────────────┤
│ • 性能强劲 [📋] │ • 价格昂贵   [📋] │
│ • 续航优秀 [📋] │ • 接口较少   [📋] │
│ • 屏幕出色 [📋] │                     │
└──────────────────┴─────────────────────┘
```

**样式**:
- 两列布局: `grid grid-cols-2 gap-4`
- 优点: 绿色 icon (`text-green-500`)
- 缺点: 红色 icon (`text-red-500`)
- 列表: 使用 `ul` + `li`

---

**生成时间**: 2025-11-11  
**规范版本**: 250-wf-prompt-engineering.mdc v2.2  
**任务优先级**: P0 (High) - V3.0 MVP "最后一公里"任务

